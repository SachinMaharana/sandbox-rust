use std::io;

fn main() {
    // use std::io::BufRead;
    // loop {
    //     let stdin = io::stdin();
    //     let reader = io::BufReader::new(stdin);
    //     let res = reader
    //         .lines()
    //         .map(|l| l.unwrap().trim().parse::<i32>().unwrap())
    //         .take(8)
    //         .enumerate()
    //         .max_by_key(|&(idx, e)| e)
    //         .unwrap()
    //         .0;
    //     println!("{:?}", res); // The number of the mountain to fire on.
    // }
}
